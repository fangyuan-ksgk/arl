# ============================================================
# Minimal GPU environment for TRL + vLLM colocate GRPO
# RunPod: CUDA 12.8 base image (torch 2.8.0+cu128 pre-installed)
# ============================================================
#
# Single install command — no torch reinstall needed:
#
#   pip install -r requirements.txt
#
# vLLM 0.11.0 is compatible with the base torch 2.8.0+cu128,
# so the CUDA binding is preserved automatically.
#
# Verified working environment:
#   torch        2.8.0+cu128
#   vllm         0.11.0
#   trl          1.1.0
#   transformers 4.56.1
#   CUDA         12.8

vllm==0.11.0
trl==1.1.0
transformers==4.56.1
accelerate
datasets
peft
matplotlib
seaborn
pyarrow
pandas